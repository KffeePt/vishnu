import { APP_VERSION } from '../utils/app-version';

export const AppConfig = {
    name: 'CodeMan',
    version: APP_VERSION,
    emojis: {
        default: '⚡',
        nextjs: '⚛️',
        flutter: '🦋'
    }
};
